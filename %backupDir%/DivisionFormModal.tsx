// Shim: re-export the JSX implementation. Keep this file JS-compatible so the
// linter won't fail if the repo is JSX-only.
import DivisionFormModalImpl from './DivisionFormModal.jsx';
export default DivisionFormModalImpl;
